# ✅ 5. MIXED CONCEPT QUESTIONS (ADVANCED LEVEL)
# Q1. Create a class BankAccount with:

# constructor (name, balance)

# instance method → deposit, withdraw

# class method → change bank name

# static method → validate account number (length 12)

# Q2. Create a class Exam with:

# constructor → student name & 3 marks

# instance method → calculate total, average

# class method → change passing marks

# static method → check if marks are valid (0–100)

# Q3. Create a class ShoppingApp with:

# constructor → user name

# instance method → add items to cart

# class variable → total_users

# class method → show total users

# static method → check if coupon code is valid

# Q4. Create class Vehicle with:

# constructor → mileage & fuel

# instance → max_distance()

# class method → change fuel price

# static method → check if speed is overspeeding

# Q5. Create class Library with:

# constructor → books list

# instance → add_book(), remove_book()

# class variable → total_libraries

# static method → check if book name is valid (letters and spaces only)